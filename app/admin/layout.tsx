export const metadata = {
  title: 'JHCO Admin Panel',
  description: 'Admin panel for JHCO content management',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
